# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real private keys.

# Private key used by the dev signer (localSigner.ts).
# If blank or missing, falls back to Hardhat account #0 (test-only key).
DEV_PRIVATE_KEY=
