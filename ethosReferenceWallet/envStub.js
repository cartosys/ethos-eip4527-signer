// Metro resolves this file for the virtual @env module.
// The react-native-dotenv Babel plugin replaces all `import { X } from '@env'`
// with inlined .env values at transform time, so this file's exports are never used.
module.exports = {};
